import 'package:flutter/material.dart';

class PurchaseledgerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình bảng kê mua vào"),
      ),
    );
  }
}
