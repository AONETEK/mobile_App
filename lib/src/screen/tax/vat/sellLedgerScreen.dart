import 'package:flutter/material.dart';

class SellledgerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình bảng kê bán ra"),
      ),
    );
  }
}
