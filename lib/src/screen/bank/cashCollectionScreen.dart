import 'package:flutter/material.dart';

class CashCollectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình thu tiền mặt"),
      ),
    );
  }
}
