import 'package:flutter/material.dart';

class DepreciationOfFixedassetsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình khấu hao tài sản cố định"),
      ),
    );
  }
}
