import 'package:flutter/material.dart';

class CorporateIncomesTaxScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình thuế thu nhập doanh nghiệp"),
      ),
    );
  }
}
