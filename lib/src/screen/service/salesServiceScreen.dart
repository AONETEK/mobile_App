import 'package:flutter/material.dart';

class SalesServiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình dịch vụ bán ra"),
      ),
    );
  }
}
