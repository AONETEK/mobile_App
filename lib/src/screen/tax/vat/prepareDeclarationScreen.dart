import 'package:flutter/material.dart';

class PrepareDeclarationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình lập tờ khai"),
      ),
    );
  }
}
