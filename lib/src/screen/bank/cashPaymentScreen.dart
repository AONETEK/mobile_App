import 'package:flutter/material.dart';

class CashPaymentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình chi tiền mặt"),
      ),
    );
  }
}
