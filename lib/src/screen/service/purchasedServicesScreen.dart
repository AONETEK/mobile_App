import 'package:flutter/material.dart';

class PurchasedServicesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Màn hình dịch vụ mua vào"),
      ),
    );
  }
}
